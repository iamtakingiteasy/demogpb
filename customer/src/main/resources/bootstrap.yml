spring:
  application:
    name: customer

  cloud:
    config:
      enabled: true
      #profile: production
      #label: master
      discovery:
        enabled: true
        service-id: config-service
      failFast: false
      retry:
        initialInterval: 3000
        maxAttempts: 100
