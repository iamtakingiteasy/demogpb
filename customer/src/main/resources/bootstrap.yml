spring:
  aop:
    proxyTargetClass: true
  application:
    name: customer
  profiles:
    active: dev

  cloud:
    config:
      enabled: false
      uri: http://config-service:8888
      failFast: false
      retry:
        initialInterval: 3000
        maxAttempts: 100