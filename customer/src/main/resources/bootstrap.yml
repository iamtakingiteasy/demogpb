spring:
  application:
    name: customer

  cloud:
    config:
      enabled: true
      uri:
      #profile: production
      #label: master
      discovery:
              enabled: true
              service-id: config-service
