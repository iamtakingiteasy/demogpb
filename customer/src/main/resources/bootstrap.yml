spring:
  application:
    name: customer

  cloud:
    config:
      enabled: false
      uri:
      #profile: production
      #label: master
      discovery:
              enabled: true
              service-id: config-service
