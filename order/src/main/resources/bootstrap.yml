spring:
  application:
    name: order

  cloud:
    config:
      enabled: true
      uri: http://config-service:8888
      #profile: production
      #label: master
#      discovery:
#              enabled: true
#              service-id: config-service
      failFast: false
      retry:
        initialInterval: 3000
        maxAttempts: 100
