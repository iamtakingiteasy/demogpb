info:
  version: 1.0.0
  description: подсистема "Реестр сервисов"

server:
  context-path:
  port: 8761

eureka:
  instance:
    prefer-ip-address: false
  client:
    registerWithEureka: false
    fetchRegistry: false
