spring:
  application:
    name: catalog

  cloud:
    config:
      enabled: true
      #profile: production
      #label: master
      discovery:
              enabled: true
              service-id: config-service
