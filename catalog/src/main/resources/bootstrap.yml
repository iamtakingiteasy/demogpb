spring:
  application:
    name: catalog

  cloud:
    config:
      enabled: true
      uri: http://config-service:8888
      #profile: production
      #label: master
#      discovery:
#              enabled: true
#              service-id: config-service
