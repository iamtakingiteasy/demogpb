spring:
  application:
    name: catalog

  cloud:
    config:
      enabled: true
      uri:
      #profile: production
      #label: master
      discovery:
              enabled: true
              service-id: config-service
